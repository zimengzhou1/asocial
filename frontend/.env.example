# Copy this file to .env.local and configure for your environment

# Backend WebSocket URL (development only)
# In production, WebSocket connects to the same host via Traefik
NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:3001/api/chat

# Backend HTTP URL (if needed for API calls)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
