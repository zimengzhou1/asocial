apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: asocial
type: Opaque
stringData:
  # ⚠️  DEVELOPMENT ONLY - DO NOT USE IN PRODUCTION
  # These credentials are ONLY for local minikube testing
  # For production, create secrets manually (see secret.yaml.template)
  POSTGRES_USER: "asocial"
  POSTGRES_PASSWORD: "asocial_dev_password"
  POSTGRES_DB: "asocial"
