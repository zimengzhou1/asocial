apiVersion: v1
kind: Secret
metadata:
  name: firebase-credentials
  namespace: asocial
type: Opaque
stringData:
  # ⚠️  DEVELOPMENT ONLY - DO NOT USE IN PRODUCTION
  # This is a placeholder for local minikube/k3s testing
  # For actual Firebase credentials, use the command below:
  #
  # kubectl create secret generic firebase-credentials \
  #   --from-file=serviceAccount.json=/path/to/your/firebase-dev-credentials.json \
  #   -n asocial
  #
  # Or if you want to include it here directly (NOT RECOMMENDED):
  # serviceAccount.json: |
  #   {
  #     "type": "service_account",
  #     "project_id": "your-project",
  #     ...
  #   }
  serviceAccount.json: "{}"  # Empty placeholder - will fail auth until replaced
